apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: safeway-kids
  labels:
    app: web
    app.kubernetes.io/part-of: safeway-kids
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: web
          image: safeway-kids-web:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: safeway-kids
  labels:
    app: web
    app.kubernetes.io/part-of: safeway-kids
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
