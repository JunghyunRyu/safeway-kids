# M3 Session Handoff Packet

**Date**: 2026-03-13
**Milestone completed**: M3 — Real-time Features Hardening

## Current Status
M0–M3 complete. Backend has 13 API endpoints across 6 domain modules. Mobile app has parent and driver flows with role-based navigation, Kakao Maps, GPS tracking, and push notification registration.

## Changed Files (M3)

### New
- `mobile/assets/kakao-map.html` — Kakao Maps JS SDK integration HTML
- `mobile/src/hooks/useVehicleTracking.ts` — WebSocket GPS subscription with exponential backoff

### Modified
- `mobile/app.json` — permissions, plugins, app name
- `mobile/src/hooks/useGpsTracking.ts` — real expo-location instead of mock
- `mobile/src/hooks/useNotifications.ts` — expo-notifications + FCM token
- `mobile/src/screens/parent/MapScreen.tsx` — live bus tracking via WebView
- `mobile/src/screens/driver/MapScreen.tsx` — GPS + stop markers via WebView
- `mobile/src/navigation/RootNavigator.tsx` — useNotifications on auth

## Commands Executed
```
cd mobile && npx tsc --noEmit           → 0 errors
cd backend && pytest tests/ -v          → 35/35 passed
```

## Test Outcomes
- Backend: 35/35 passed (unit + integration)
- Mobile: TypeScript strict mode passes with 0 errors
- No runtime/device tests (Expo dev server not run)

## Open Issues
1. **Placeholder API keys**: `YOUR_KAKAO_JS_API_KEY` in MapScreens, `your-expo-project-id` in useNotifications
2. **WebSocket backend**: Parent MapScreen expects `ws://{host}/ws/vehicle/{id}/gps` — endpoint not yet implemented
3. **Background GPS**: Only foreground tracking; background task (expo-task-manager) not wired
4. **i18n gaps**: Some strings still hardcoded in Korean (e.g., "연결됨" in parent MapScreen)

## Suggested Next Steps (M4 candidates)
1. **WebSocket GPS endpoint** — FastAPI WebSocket at `/ws/vehicle/{id}/gps` reading from Redis pub/sub
2. **VRP-TW route optimization** — OR-Tools solver for daily vehicle-to-stop assignment
3. **Alembic migration run** — Apply M2 migration on real PostgreSQL
4. **Admin dashboard** — Web UI for academy/platform admins
5. **Device testing** — Run on physical Android/iOS with real API keys

## Next Exact First Step
Decide M4 scope. If WebSocket endpoint: create `backend/app/modules/vehicle_telemetry/ws.py` with FastAPI WebSocket route that subscribes to Redis channel `vehicle:{id}:gps` and forwards updates to connected parents.
