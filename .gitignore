# Secrets & Keys
.env
.env.*
secrets/
config/credentials.json
*.key
*.key.pub
firebase-credentials.json
.mcp.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Node
node_modules/
.expo/

# Claude local overrides
.claude/settings.local.json
.claude/scheduled_tasks.lock
CLAUDE.local.md

# Local scratch output
tmp/
.tmp_drive_readmes/
dist/
build/
coverage/
*.log

# Loose debug screenshots at repo root (move to artifacts/ if needed)
/c14-*.png

# ML model files (too large for GitHub)
*.onnx

# Uploaded files
uploads/

# Edge AI registered face data (PII / 생체정보 — 절대 커밋 금지)
edge_ai/registered_faces/

# OS/editor
.DS_Store
.vscode/
.idea/
*.db

vmware_ubuntu.key
vmware_ubuntu.key.pub

# Firebase Admin SDK key (서버 전용 — 절대 커밋 금지)
safeway-kids-firebase-adminsdk-fbsvc-0e13eee2eb.json
# google-services.json, GoogleService-Info.plist는 클라이언트 설정으로 커밋 가능.playwright-mcp/
.playwright-mcp/
*.tsbuildinfo
